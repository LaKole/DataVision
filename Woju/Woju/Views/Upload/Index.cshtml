
@{
    ViewBag.Title = "Index";
}

<h2>Step One</h2>
<p>Please upload/select your dataset and select a visual format once prompted</p>

<p></p>

<h3>Upload Dataset (for now only csv files accepted)</h3>
@using (Html.BeginForm("Upload", "Upload", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <p>Submission name, will be used as table name in DB. before entry checks will be run to ensure not already existing</p>
    <input type="text" id="submissionName" , name="submissionName" />
    <input type="file" name="file" id="file" />
    //<input type="file" name="file1" id="file"/>
    <input type="submit" value="Upload" />
}

<p></p>
<p></p>
<p></p>

<h3>Select Existing Dataset</h3>
@using (Html.BeginForm("Index", "Query")) //, FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <p>once existing dataset (already loaded in database, searchable by submission name (table name))is selected users taken to Query handler page</p>
    <input type="file" name="file" id="file" />
    <p>current button file upload, will change this once dbconnection string issues resolved - will list existing tables name linked to userID (eg previous submissions for this user)</p>
    <input type="submit" value="Select" />
    <p>select will pass in datasetID to be used in query handler</p>
}

<p></p>
<p></p>
<p></p>





